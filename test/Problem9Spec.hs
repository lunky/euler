module Problem9Spec where
import Problem9
import Test.Hspec

spec :: Spec
spec = do
  describe "Problem9" $ 
    it "should test the solution" $ 
    pendingWith "I can't really think of a way to test this without causing it to try to solve the problem."
